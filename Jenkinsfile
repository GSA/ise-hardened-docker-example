@Library('ise-jenkins-shared-library') _ // visit shared library source code for usage info: https://github.com/GSA/ise-jenkins-shared-library/tree/main/vars

hardenDockerImage "${params.artifactoryURL}", "${params.artifactoryRegistry}", "${params.artifactoryRegistryCredID}", "${params.pushImageName}", "${params.pushImageTag}", "${params.dockerFile}", "${params.twistlockURL}", "${params.twistlockCredID}", "${params.anchoreURL}", "${params.anchoreCredID}"
